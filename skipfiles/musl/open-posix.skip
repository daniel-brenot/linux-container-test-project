# Open POSIX tests skipped on the musl (Alpine) image only.
# One test name per line (basename without .run-test).
#
# Requires SCHED_FIFO / root privileges (also fails on glibc unprivileged):
sigaction_16-1
# Hangs / times out under musl (SIGSTOP path):
sigaction_9-1
# musl accepts invalid signal numbers in sigaddset (POSIX expects EINVAL):
sigismember_5-1
# Hangs under musl:
sigpause_1-2
sigpause_4-1
# si_code not populated as this test expects under musl:
sigwaitinfo_6-1
